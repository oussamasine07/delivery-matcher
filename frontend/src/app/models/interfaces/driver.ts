export interface Driver {
  id: number | null,
  firstName: string | null,
  lastName: string | null,
  email: string | null,
  numberOfTrucks: number | null,
  city: string | null,
  phone: string | null
}
